<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RickAndMortyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:windowSoftInputMode="adjustResize">
        <activity android:name=".data.EpisodeDetails"></activity>
        <activity android:name=".ui.episodes.EpisodeListActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.welcome.WelcomeActivity" />
        <activity android:name=".ui.choose_path.ChoosePathActivity" />
        <activity android:name=".ui.seasons.SeasonsActivity" />
        <activity android:name=".ui.quiz.QuizListActivity" />
        <activity android:name=".ui.episodes.EpisodesPlayerActivity" />
        <activity android:name=".ui.videoplayer.VideoPlayerActivity" />
        <activity
            android:name=".ui.quiz.QuizQuestionActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>